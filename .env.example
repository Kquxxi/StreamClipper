##############################
# StreamClipper – Example .env
# Copy to .env and adjust values
##############################

# --- Server ---
PORT=5001
HOST=127.0.0.1
DEBUG=true
ENABLE_SCHEDULER=false

# Public tunnel base URL (no trailing slash), e.g. Cloudflared
# PUBLIC_BASE_URL=https://your-tunnel.example

# Data directory for runtime files (defaults to project root if empty)
# DATA_DIR=C:\\Users\\YourUser\\StreamClipperData

# --- Twitch ---
TWITCH_CLIENT_ID=<your_client_id>
TWITCH_CLIENT_SECRET=<your_client_secret>
# Optional report tuning
TWITCH_WINDOW_HOURS=24
TWITCH_MIN_VIEWS=0
TWITCH_REQUEST_TIMEOUT=20
TWITCH_MAX_PAGES=20
TWITCH_MIN_FOLLOWERS=0
TWITCH_REPORT_MAX_STREAMERS=200

# --- Kick ---
# Choose ONE source for cookies
# From browser (chrome|edge|firefox|brave|chromium)
# KICK_COOKIES_FROM_BROWSER=chrome
# Or from Netscape cookie file path
# KICK_COOKIES_FILE=C:\\path\\to\\cookies.txt
# Optional UA override
# KICK_USER_AGENT=Mozilla/5.0 (...)

# --- Publer ---
# API key and workspace
# PUBLER_API_KEY=<your_api_key>
# PUBLER_WORKSPACE_ID=<workspace_id>
# Either a single account ID or comma-separated list
# PUBLER_ACCOUNT_ID=<account_id>
# PUBLER_ACCOUNT_IDS=<id1,id2,id3>

# --- Transcription (WhisperX) ---
# Optional HF token
# HF_TOKEN=<hf_token>
TRANSCRIBE_PYTHON=python
TRANSCRIBE_TIMEOUT_SEC=600
TRANSCRIBE_LANGUAGE=pl
TRANSCRIBE_MODEL=medium
TRANSCRIBE_DIAR=false

# --- Paths overrides (optional) ---
# TWITCH_DB_FILE=database.json
# STREAMERS_DB_FILE=streamers_prefs.json
# TWITCH_DATABASE_FILE=database.json
# KICK_DATABASE_FILE=kick/kick_database.json

# --- Notes ---
# - Do NOT commit real secrets. .env is ignored via .gitignore
# - If using Cloudflared, set PUBLIC_BASE_URL to your tunnel hostname
# - Adjust PORT/DEBUG via shell or here as needed
