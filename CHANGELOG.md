# Changelog

## v1.0.0 (2025-12-13)

- Dodano `.env.example` z pełną listą zmiennych środowiskowych.
- Uzupełniono `README.md`: konfiguracja `.env`, szybki start, sekcja Endpoints.
- Dodano sekcję „Uruchom przez Cloudflared” w `README.md`.
- Uzupełniono `requirements.txt` o `yt-dlp` i `imageio-ffmpeg`.
- Dodano pliki wzorcowe: `config.cloudflared.example.yml`, `database.example.json`.
- Zweryfikowano `.gitignore` (cookies, media, raporty, logi, cache, locks); propozycje dalszych uzupełnień — patrz README/sekcja decyzji.

> Uwaga: wersja v1.0.0 jest przygotowaniem repo do publikacji; finalne decyzje dot. licencji, widoczności i stabilizacji wersji zależności do uzgodnienia.

## v1.0.1 (2025-12-13)

- Test: weryfikacja pojawiania się commitów w GitHub Activity po zweryfikowaniu adresu e‑mail. Bez zmian funkcjonalnych.
